 * Unicode support. I currently assume that all strs are bytes, and that all
   bytes are ASCII text
 * Multi-process support
 * askpass support
 * py3 support (just more bytes()?)
 * item creation
 * unit tests
 * `.cloudkeychain` support
